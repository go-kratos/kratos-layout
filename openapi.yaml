# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: TodoService API
    description: TodoService provides a simple CRUD example.
    version: 0.0.1
paths:
    /v1/todos/create:
        post:
            tags:
                - TodoService
            description: CreateTodo creates a todo item.
            operationId: TodoService_CreateTodo
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/todo.v1.Todo'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/todo.v1.Todo'
    /v1/todos/list:
        get:
            tags:
                - TodoService
            description: ListTodos lists todo items.
            operationId: TodoService_ListTodos
            parameters:
                - name: pageSize
                  in: query
                  schema:
                    type: integer
                    format: int32
                - name: pageToken
                  in: query
                  schema:
                    type: string
                - name: filter
                  in: query
                  description: |-
                    Optional. The standard list filter.
                     Supported fields:
                        * `title` (i.e. `title:"bug"`)
                        * `content` (i.e. `content:"docs"`)
                        * `completed` (i.e. `completed` or `NOT completed`)
                        * `create_time` range (i.e. `create_time>="2026-01-01T00:00:00Z"`)
                  schema:
                    type: string
                - name: orderBy
                  in: query
                  description: |-
                    Optional. A comma-separated list of fields to order by.
                     Supported fields:
                        * `id`
                        * `title`
                        * `create_time`
                        * `update_time`
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/todo.v1.TodoSet'
    /v1/todos/sync:
        post:
            tags:
                - TodoService
            description: SyncTodos exchanges todo changes in both directions.
            operationId: TodoService_SyncTodos
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/todo.v1.SyncTodoRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/todo.v1.TodoEvent'
    /v1/todos/update:
        put:
            tags:
                - TodoService
            description: UpdateTodo updates a todo item.
            operationId: TodoService_UpdateTodo
            parameters:
                - name: updateMask
                  in: query
                  schema:
                    type: string
                    format: field-mask
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/todo.v1.Todo'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/todo.v1.Todo'
    /v1/todos/watch:
        get:
            tags:
                - TodoService
            description: WatchTodos streams todo snapshots from the server to the client.
            operationId: TodoService_WatchTodos
            parameters:
                - name: pageSize
                  in: query
                  schema:
                    type: integer
                    format: int32
                - name: pageToken
                  in: query
                  schema:
                    type: string
                - name: filter
                  in: query
                  schema:
                    type: string
                - name: orderBy
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/todo.v1.TodoEvent'
    /v1/todos/{id}:
        get:
            tags:
                - TodoService
            description: GetTodo returns a todo item by id.
            operationId: TodoService_GetTodo
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/todo.v1.Todo'
        delete:
            tags:
                - TodoService
            description: DeleteTodo deletes a todo item.
            operationId: TodoService_DeleteTodo
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content: {}
components:
    schemas:
        todo.v1.SyncTodoRequest:
            type: object
            properties:
                action:
                    type: string
                todo:
                    $ref: '#/components/schemas/todo.v1.Todo'
                id:
                    type: string
                updateMask:
                    type: string
                    format: field-mask
        todo.v1.Todo:
            type: object
            properties:
                id:
                    type: string
                title:
                    type: string
                content:
                    type: string
                completed:
                    type: boolean
                createTime:
                    type: string
                    format: date-time
                updateTime:
                    type: string
                    format: date-time
        todo.v1.TodoEvent:
            type: object
            properties:
                action:
                    type: string
                todo:
                    $ref: '#/components/schemas/todo.v1.Todo'
                eventTime:
                    type: string
                    format: date-time
        todo.v1.TodoSet:
            type: object
            properties:
                todos:
                    type: array
                    items:
                        $ref: '#/components/schemas/todo.v1.Todo'
                nextPageToken:
                    type: string
tags:
    - name: TodoService
