package ent

// Add --feature flags here as the schema needs them, e.g. sql/upsert for
// OnConflict support or sql/execquery for raw statements.
//go:generate go run -mod=mod entgo.io/ent/cmd/ent generate ./schema
